import { Button, Input } from "../ui";

const LoginForm = () => {
  return (
    <div>
      <Input type="email" />
      <Button type="button">Login here</Button>
    </div>
  );
};

export default LoginForm;
